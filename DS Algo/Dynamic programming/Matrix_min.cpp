#include<bits/stdc++.h>
using namespace std;

//Matrix multiplication optimize
//dp[i][j] = min to compute i...j multiplication

int matrix_multiply_dp(vector<pair<int, int> > &v)
{
	for(int i = 0; i < v.size(); i++)
		for(int j = i + 1; j < v.size(); j++)
		{
			for(int k = i; k < )
		}
	
}

int main()
{
	
}
